version: '3.8'

services:
  store-intelligence:
    build: .
    container_name: store_intelligence_system
    ports:
      - "8000:7860"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
